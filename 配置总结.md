# ✅ OpenCode Go 迁移已完成

## 📝 已完成的工作

### 1. 配置更新 ✅
- **API Endpoint**: `https://opencode.ai/zen/go/v1`
- **模型**: `deepseek-v4-flash` (DeepSeek V4 Flash)
- **API Key**: 已配置到 `.env.local`

### 2. 已修改的项目文件 ✅
| 文件 | 修改内容 |
|------|---------|
| `.env.local` | 更新 API Key, BASE_URL, MODEL_NAME |
| `.env.example` | 更新示例配置，改为 OpenCode Go |
| `lib/server/env.ts` | 默认模型改为 `deepseek-v4-flash` |
| `README.md` | 更新文档说明使用 OpenCode Go |

### 3. 新增的辅助文件 ✅
| 文件 | 用途 |
|------|------|
| `test-api.mjs` | Node.js API 测试脚本 |
| `test-api.sh` | Bash API 测试脚本 |
| `MIGRATION.md` | 详细的迁移说明文档 |
| `GIT-PUSH-GUIDE.md` | Git 推送和 Vercel 配置指南 |
| `配置总结.md` | 本文件 |

---

## 🚀 接下来你需要做的事情

### 第一步：测试 API（必须！）

在推送代码之前，**必须先测试** API 是否正常工作：

```bash
cd B:/sharewithlight/lifeKLINE
node test-api.mjs
```

预期输出：
```
✅ API 测试成功！
AI 回复: 你好！有什么可以帮助你的吗？
```

**如果测试失败**，请检查：
1. 网络连接是否正常
2. OpenCode Go 账户是否有余额
3. API Key 是否正确

---

### 第二步：本地开发测试（推荐）

启动开发服务器，测试完整流程：

```bash
pnpm dev
```

然后：
1. 访问 http://localhost:3000
2. 填写生日信息（例如：1995-06-15）
3. 选择性别、维度（财富）、周期（年度）
4. 点击"生成人生 K 线"
5. 等待 30-60 秒
6. 检查是否显示：
   - ✅ K 线图表
   - ✅ 技术指标（MACD、KDJ、RSI、BOLL）
   - ✅ 分析文本

**如果本地测试成功，继续下一步！**

---

### 第三步：推送到 GitHub

```bash
cd B:/sharewithlight/lifeKLINE

# 查看修改
git status

# 添加文件
git add .env.example lib/server/env.ts README.md MIGRATION.md test-api.sh test-api.mjs GIT-PUSH-GUIDE.md

# 提交
git commit -m "feat: migrate to OpenCode Go with DeepSeek V4 Flash

- Update API endpoint to https://opencode.ai/zen/go/v1
- Switch model from Qwen to DeepSeek V4 Flash
- Update default model in env.ts to deepseek-v4-flash
- Add API test scripts and migration documentation"

# 推送（注意：你的分支是 master）
git push origin master
```

**⚠️ 重要**: 不要推送 `.env.local` 文件！

---

### 第四步：更新 Vercel 环境变量

1. 访问 https://vercel.com/dashboard
2. 选择 `lifekline` 项目
3. Settings → Environment Variables
4. 更新这三个变量（Production, Preview, Development 都要勾选）：

```
ALI_BAILIAN_API_KEY = sk-ykrhEuzmR7Egf2wUmq0OWubAltpSYNx2NpubuhdQhWOoytpp0ANl3ozaQqueIo6z
ALI_BAILIAN_BASE_URL = https://opencode.ai/zen/go/v1
ALI_BAILIAN_MODEL_NAME = deepseek-v4-flash
```

5. 保存后，Vercel 会自动重新部署

---

### 第五步：验证生产环境

等待 Vercel 部署完成后：

1. 访问 https://labklife.lightinglab.top
2. 测试生成一个人生 K 线
3. 检查是否正常工作

**如果正常显示，迁移完成！🎉**

---

## 📊 配置对比

| 配置项 | 旧值（阿里百炼） | 新值（OpenCode Go） |
|--------|----------------|-------------------|
| BASE_URL | `https://coding.dashscope.aliyuncs.com/v1` | `https://opencode.ai/zen/go/v1` |
| MODEL | `qwen3-max-2026-01-23` | `deepseek-v4-flash` |
| API Key | `sk-sp-feae...` (已过期) | `sk-ykrhEuz...` (新) |

---

## 💡 技术说明

### 为什么选择 DeepSeek V4 Flash？
1. **速度快** - Flash 版本针对低延迟优化
2. **成本低** - 比 Pro 版本便宜，适合高频调用
3. **中文能力强** - 专为中文优化，适合八字分析
4. **完全兼容** - 使用 OpenAI 兼容 API，无需改代码

### 代码兼容性
你的项目使用标准的 OpenAI Chat Completions API 格式，所以：
- ✅ 无需修改 `fetch` 调用
- ✅ 无需修改请求/响应格式
- ✅ 只需更改 BASE_URL 和 MODEL_NAME

---

## 🔍 故障排查

### 问题：本地测试失败
- 检查 `.env.local` 文件是否存在
- 检查环境变量值是否正确
- 运行 `node test-api.mjs` 看详细错误

### 问题：Vercel 部署失败
- 检查 Vercel 环境变量是否都设置了
- 查看 Vercel Function Logs
- 确认 BASE_URL 末尾没有多余的斜杠

### 问题：API 返回 401
- API Key 错误或过期
- 检查是否正确复制了完整的 Key

### 问题：API 返回 404
- BASE_URL 错误
- 应该是 `https://opencode.ai/zen/go/v1` (注意是 `/zen/go/v1`)

---

## 📞 需要帮助？

如果遇到问题：
1. 查看 `MIGRATION.md` - 详细的迁移说明
2. 查看 `GIT-PUSH-GUIDE.md` - Git 和 Vercel 操作指南
3. 运行测试脚本查看详细错误信息
4. 检查 Vercel Function Logs

---

## ✅ 完成检查清单

- [x] 更新 `.env.local`
- [x] 更新 `.env.example`
- [x] 更新 `lib/server/env.ts`
- [x] 更新 `README.md`
- [x] 创建测试脚本
- [x] 创建迁移文档
- [ ] **运行 `node test-api.mjs` 测试 API**
- [ ] **运行 `pnpm dev` 测试本地开发**
- [ ] **推送到 GitHub**
- [ ] **更新 Vercel 环境变量**
- [ ] **验证生产环境**

---

**现在，请从"第一步：测试 API"开始执行！** 🚀
